# 依赖与构建产物
node_modules/
dist/
coverage/
*.tsbuildinfo

# 环境与密钥(零硬编码密钥铁律)
.env
.env.*
!.env.example

# 本地 SQLite 开发库
*.db
*.db-journal
prisma/dev.db
prisma/migrations/dev.db*

# 本地 SQLite 开发产物(由 scripts/gen-sqlite-schema.cjs 生成,非契约基准)
# 契约基准始终是 prisma/schema.prisma(PostgreSQL)。
prisma/schema.sqlite.prisma
# SQLite 临时迁移(provider=sqlite),正式 Postgres 迁移由 devops 生成
prisma/migrations/

# 日志
*.log
