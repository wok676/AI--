# 依赖与构建产物
node_modules/
dist/
coverage/
*.tsbuildinfo

# 环境与密钥(零硬编码密钥铁律)
.env
.env.*
!.env.example

# 本地 SQLite 开发库
*.db
*.db-journal
prisma/dev.db
prisma/migrations/dev.db*

# 本地 SQLite 开发产物(由 scripts/gen-sqlite-schema.cjs 生成,非契约基准)
# 契约基准始终是 prisma/schema.prisma(PostgreSQL)。
prisma/schema.sqlite.prisma
# 注意:正式 Postgres 迁移(prisma/migrations/**/migration.sql)**必须入库**——
# 它是 CI 测试建表与生产 migrate deploy 的唯一依据;此前整目录被忽略导致
# CI/生产 migrate deploy 找不到迁移、建不出表(已修复)。仅忽略本地 SQLite 临时库。
prisma/migrations/**/dev.db*

# 日志
*.log
