# ============================================================
# TestAI admin console env sample — 占位无真值(宪法 §1.4/§6)
# 复制为 .env 填真实值;.env 已被 .gitignore 忽略,严禁提交。
# ============================================================

# 后端 API 基址(含 /api/v1 前缀)。生产填公网 HTTPS 地址。
# 开发留空时走 vite 代理 /api -> VITE_DEV_API_TARGET。
VITE_API_BASE_URL=

# 仅开发期:vite dev server 把 /api 代理到此后端。
VITE_DEV_API_TARGET=http://localhost:3000
