# 数据库模型(Prisma)

> 状态:**TBD(待定题)**。由 `architect` 与 `backend-engineer` 据 PRD 设计。

定题后本文件需包含:
- 各实体表(字段、类型、约束)
- 关系(1:N / N:N)与外键
- 索引(按查询热点,如 status / ownerId)
- 角色枚举与权限相关字段(支撑鉴权)

> 对应的 Prisma schema 落在 `server/prisma/schema.prisma`。
